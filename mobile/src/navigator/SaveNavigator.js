import React from 'react';
import { SafeAreaView, Text } from 'react-native';
import Screen from '../screens/Screen';

function SaveNavigator(props) {
    return (
        <Screen>
            <Text style={{color: '#000'}}>SaveNavigator</Text>
        </Screen>
    );
}

export default SaveNavigator;